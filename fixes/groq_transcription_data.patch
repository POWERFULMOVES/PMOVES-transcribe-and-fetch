diff --git a/backend/app/transcribe1.py b/backend/app/transcribe1.py
index abc1234..def5678 100644
--- a/backend/app/transcribe1.py
+++ b/backend/app/process_audio_with_groq.py
@@ -562,10 +562,18 @@ async def process_audio_with_groq(
                                 full_text += text + " "
                                 
                                 # Stream the text immediately with timestamp and link
-                                await transcription_queue.put(json.dumps({
+                                # Use proper JSON structure instead of markdown text
+                               await transcription_queue.put(json.dumps({
                                     "type": "transcription_segment",
-                                    "content": f"| [{segment_dict['start']}]({watch_url}) | {video_id} | {len(segments)} | {segment_dict['start']} | {segment_dict['end']} | {text} |"
+                                    "content": {
+                                        "text": text,
+                                        "start_time": start_ms / 1000,
+                                        "end_time": end_ms / 1000,
+                                        "id": len(segments),
+                                        "video_id": video_id,
+                                        "watch_url": watch_url,
+                                        "timestamp": segment_dict['start']
+                                    }
                                 }))
                                 
                                 logger.info(f"Streamed text: {text[:50]}...")